# Normal usage requires the Python SQLite API (npm run server).
# Guest-only builds do not synchronize to SQLite:
VITE_PUBLIC_ACCESS=false
# For the Python process, configure PORT=5005 and DB_PATH=/data/cografya.db
# in the deployment environment. Python does not read Vite's .env files.
